# mmemo 桌面版验收

## 2026-09-17 紧凑对话版（当前）

- 面板窗口 392 × 448，四周 8pt 透明边距，内容四边 14px 留白；列表行高从 78px 缩为 48px，标题从 20px 缩为 13px。
- 参考本地 d-sre-agent 的 `theme.css`、Button/Input/Textarea：中性灰、黑白主按钮、细边框、小圆角；没有为样式引入 React 或组件库依赖。
- 原生毛玻璃使用 hudWindow + behindWindow，模糊层 alpha 保持 1；移除网页面板额外的白色遮罩，保留输入框的轻量填充和清晰文字。先前降低模糊层 alpha 的方式已撤销。
- 移除新增表单、日期控件、编辑弹层和列表内写操作，统一为底部 AI 输入。支持 Enter 发送、Shift+Enter 换行和输入法组合保护。
- 青蛙增加 `acceptsFirstMouse` 和辅助功能 press 动作；青蛙窗口不再获得键盘焦点，输入面板仍可成为 key window。
- 实际重启后单次点击打开，中文粘贴、回车发送、请求中禁用重复提交、收到真实 AI 回复、清空成功发送的输入均已检查。Computer Use 中文逐字输入丢字，改用粘贴后在 AX 中核对完整内容。
- 截图：`docs/qa/compact-ai-panel.png`，真实桌面应用与真实模型回复。单窗口捕获不包含桌面背景，透明度需要在实际桌面上观察。
- 真实模型联调：无用户数据问候 canary；在独立临时目录验证新增带时间事项、完成、删除和实际磁盘保存。此前发现模型把日期塞进标题而漏填 due，已收紧工具字段；操作回执改由实际落盘数据生成。最终完整 4 请求场景通过，含调试及桌面对话共 11 次模型请求。
- 自动检查通过：JS 语法与模型检查、Swift 存储检查、模型操作校验（未知 ID、重复变更、无效日期、遗漏日期、批次原子性）、构建和签名。
- 用户原有待办未被验收数据覆盖。模型配置为应用外 0600 文件；云端同步不在本轮验证范围。

以下为之前版本的历史验收，旧的新增/编辑界面已被本轮替换。

## 历史版本：本地手动待办

final result: passed

## 目标与证据

- 来源：`/var/folders/q4/m_dy0g_90lj2pbc9tq6db3h40000gn/T/codex-clipboard-c09f9a6a-3656-463d-b48e-8bab2de049fd.png`，1536 × 1024，选取左上卡片作为布局参考。
- 最终实现截图：`docs/qa/panel.png`，440 × 500 像素，AppKit 窗口内 WebKit 的 440 × 500 CSS 点视口。
- 状态：亮色、四条通过真实界面录入的测试任务、一条已完成，一条设置时间；添加栏获得焦点。
- 全图比较：在同一次视觉工具输入中打开最终截图与用户参考图；源图为六方案展示板，实现为单张实际桌面卡片。按单卡比较，不把展示板尺寸当作窗口尺寸。
- 局部比较：440 像素截图中标题、圆形状态按钮、头像、时间和添加栏均清晰可读，无须进一步放大。
- 原型状态与最终内容有明确区别：本轮采用真实本地清单。待对方确认、双人头像不模拟成已接通的功能。复用用户已有 APNG 青蛙作为头像；参考图中的不同成员动物头像留给后续协作功能。

## 五项视觉检查

1. 字体：系统 PingFang SC，任务标题 20px，日期 14px，小型标题与本地保存说明降低层级；长标题可换行。
2. 间距：圆角 30px、卡片内边距 16px、行高至少 78px；四行完整显示，更多内容内部滚动；添加栏始终可见。
3. 色彩：深蓝灰标题、灰蓝时间、蓝色完成状态；NSVisualEffectView 模糊真实桌面，随底下窗口颜色变化，不绘制假壁纸。
4. 图像：复用实际 APNG 动画；操作图标由系统 SF Symbols 导出为 PNG；未用文本符号或自绘 SVG 冒充图标。
5. 文案：本地版明确显示“保存在此 Mac”，无“已同步”“已发送给 TA”等未经实现的反馈。

## 发现与修复记录

- P2：440 × 460 初版中四行时最后一行紧贴添加栏且裁切。调整为 440 × 500，最终截图四行完整，底部输入区不遮挡内容。
- P2：首次头像使用完整透明画布，青蛙显得很小。改为圆形容器中以素材右侧为中心展示，最终截图可辨认。
- 行为缺陷：拖动读取 `NSEvent.mouseLocation`，合成拖动事件期间该值不变化，被误判成点击。改用事件坐标转屏幕坐标；复测未打开面板，持久化高度从 0.65 变为 0.4800498753，右侧停靠保持。

## 实测交互

使用 Computer Use 操作真实 macOS 应用，非浏览器假桌面：

- 青蛙点击打开；Esc 关闭；面板外菜单区域点击后面板收起。
- 添加、中文编辑、回车提交、填写原生日期时间字段。
- 完成与仅显示未完成筛选，已完成任务保留。
- 删除本轮录入的测试事项；撤销恢复。
- 退出、重新启动后任务内容与完成状态保留。
- 青蛙拖动后位置偏好更新，重启后保留（见最终运行检查）。
- 中文粘贴工具返回剪贴板读取超时，但后续 AX 确認字段含完整中文，提交后磁盘数据正确；未把工具报错误判为输入丢失。

## 自动检查与边界

- JS 模型检查：空白/超长标题、状态类型、重复 ID、日期格式、非法日历日期、相对日期、HTML 作为字面文本。
- Swift 实际磁盘检查：空文件夹、保存重载、完成状态、上次有效备份、损坏文件不被当成空数据。
- 编译、JavaScript 语法、应用签名校验通过。
- WK 页面错误已接入本机日志；界面测试没有观察到错误提示。没有连接 Web Inspector 做完整控制台审计。
- 未实测外接多显示器、全屏 Space、其他 macOS 版本。代码已设置跨 Space 与屏幕变更约束，不把它们记作实测通过。

## 后续

- P3：协作版再加入另一位使用者头像与真实确认状态。
- 毛玻璃实际色彩取决于桌面背景，与设计图壁纸上的色彩不同属于运行环境差异。

最终运行检查：重新构建后启动，`heightFraction` 仍为 `0.4800498753117207`；本机任务文件为 `[]`，面板显示 0 件未完成。已保存 `docs/qa/empty.png` 与 `docs/qa/frog.png`；最终收起面板，保持青蛙常驻供用户试用。

### 原生材质透色调整

按用户确认，先移除 CSS 白色遮罩，再使用 underWindowBackground 材质保留窗后背景上下文。系统 reduceTransparency / increaseContrast 均为 false，未修改系统偏好。已使用临时彩色文字背景尝试检查；Computer Use 单窗口截图不含窗后合成内容，因此不能把灰色截图作为透色效果的验收证据。构建和签名检查通过；实际透色程度留待用户在桌面观察。

### 透色问题的运行时排查与修正

- 用户复测上一版仍不透，不能把此前的编译成功视为效果已解决。
- 运行时检查确认窗口、WKWebView 和 html/body/.panel 都透明；原生 CABackdropLayer 已包含 gaussianBlur 和 colorSaturate。没有修改任何系统私有层或滤镜。
- 当前机器 macOS 26.6.1、VibrantLight：underWindowBackground 的正常混合浅色填充 alpha 为 0.84；popover 为 0.60；hudWindow 为 0.48；sidebar 为 0.84；menu 为 0.72。另一层 alpha 为 1 的灰色使用 darkenBlendMode，并非普通不透明覆盖。圆角 layer 裁切与 maskImage 的对照没有改变上述叠色参数。
- 修正：采用适合悬浮工具窗的 hudWindow，整个效果层 alpha 仍为 1，网页背景仍透明。与最初 hudWindow + alpha 0.68 不同，这次不把未模糊的背景混回来。
- 可重复的独立诊断：`swift docs/qa/material-probe.swift`，只打开临时材质窗口并输出原生填充和滤镜，随后自动退出；不读取业务数据，不修改 AppKit 私有属性。填充值为本机观察值，不作为跨 macOS 版本的固定契约。
- 系统截图应用未能通过 Computer Use 启动，单窗口截图仍不能证明桌面合成效果；本次依据原生运行时数据定位叠色问题，不宣称完整屏幕视觉验收通过。

### 分离式正式 UI

- 待办、输入、预览为三个独立 NSPanel，HUD 毛玻璃与四周 8px 留白保留。
- 最新 Agent 回复限定三行；用户输入、错误、取消不替换回复；全文可滚动、可返回列表。
- WebKit composer 和 message-surfaces 检查通过；原生 AppDelegate 集成检查通过，覆盖三个窗口的独立身份、右侧对齐、上下不重叠、气泡点击跨窗口展示全文及收起。
- 存储、模型变更校验、爱心和构建签名检查通过。本次未发起真实模型请求。
- Computer Use 读取正式应用两次超时，本次原生交互由独立测试进程验证，未取得正式应用的桌面合成截图。


## 2026-09-17 第 3 张贴边胶囊

- 视觉目标：`/Users/pptg/.codex/generated_images/01a0ae51-b5c8-7d61-b0d7-65b37544f568/exec-6ad9c105-1881-4541-a5e5-1efbc0db8348.png`。
- 实现：原生 260×64pt 窗口，胶囊内容 244×48pt、24pt 圆角；青蛙原素材叠在右端，移除箭头和独立重阴影。文字仍可点击查看完整消息。
- 原生子窗口置于青蛙下层，拖动一同移动；列表下方空间不足时放在青蛙上方。
- 已通过 WebKit 消息表面检查、原生胶囊关联/点击/轮换/消失计时检查、构建及签名验证；本次无模型请求。
- 截图：`docs/qa/capsule-native.png`，Computer Use 返回 260×64 的单窗口捕获，不能可靠呈现多个原生窗口的桌面合成关系，不能据此宣布与参考图视觉一致。
- 全桌面视觉对比仍缺少有效捕获，待实际桌面复核；保留已有验收记录。
- final result: blocked


## 2026-09-17 改用第 1 种内容自适应气泡

- 用户最新选择覆盖第 3 种：白色圆角气泡、小尾巴、无打开图标。
- 使用系统 13pt 字体测量状态文字，窗口宽度随每次状态更新收缩或增长，额外宽度仅包含两侧 16pt 留白、边框、小尾巴和窗口透明边距。
- 气泡在青蛙左侧，点击全文与随拖动保持。
- 原生检查通过：短文字变长、长文字缩短、青蛙旁定位、低屏幕边缘避让、点击全文、状态轮换和隐藏计时；WebKit 两种表面检查、编译、签名验证通过。
- 尚未取得有效多窗口桌面合成截图，视觉对照仍未验收。
- final result: blocked
